import {Teacher} from './teacher';
import {Student} from './student';

const student = new Student('Jim', 18, 'Male', 'Run')

const teacher = new Teacher('Mia', 30, 'Female', 'Jump')

console.log(student);
console.log(teacher);